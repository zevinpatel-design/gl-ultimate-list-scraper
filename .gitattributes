# Normalize line endings on commit. Prevents Windows teammates' git autocrlf
# settings from CRLF-corrupting JS files when they clone or pull.
* text=auto eol=lf

# Binary file types — never normalize.
*.png    binary
*.jpg    binary
*.jpeg   binary
*.gif    binary
*.ico    binary
*.zip    binary
*.woff2  binary
*.pdf    binary
